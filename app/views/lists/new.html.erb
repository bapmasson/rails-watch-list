<div class="container">
  <h1>New List</h1>
  <%= simple_form_for (@list) do |f| %>
    <%= f.input :name %>
    <%= f.input :image_url %>
    <%= f.button :submit, "Create List", class: "btn btn-primary" %>
  <% end %>

  <%= link_to "Home", root_path %>
</div>
